"""K1 Boxing - remote-controlled punching for the Booster K1 (Adam).

Ported from KyleAlanJeffrey/booster (booster_python_client), which targeted the
Booster T1, and adapted for the K1 with balance-safe upper-body control.

Run with:
    python3 -m k1_boxing --verify-joints        # confirm arm indices first
    python3 -m k1_boxing --speed slow            # fight mode (slow default)
"""
